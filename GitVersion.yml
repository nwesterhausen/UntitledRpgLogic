#$schema: https://gitversion.net/schemas/6.3/GitVersion.configuration.json
workflow: GitFlow/v1
next-version: 0.7.0
mode: ContinuousDelivery
branches:
  main:
    is-release-branch: true
  develop:
    regex: "^dev(elop)?(ment)?$|^next$"
